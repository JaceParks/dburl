<table>

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.store_id}}</td>
            <td>{{this.state}}</td>
            <td>{{this.city}}</td>
            <td>{{this.zip_code}}</td>
            <td>{{this.street}}</td>
            <td>{{this.address_number}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>